import 'package:flutter/material.dart';

/// Brand palette with light blue background and dark blue navigation
class AppColors {
  // Primary colors
  static const primary = Color(0xFF1565C0); // dark blue
  static const primaryLight = Color(0xFF42A5F5); // lighter blue
  static const secondary = Color(0xFF0D47A1); // darker blue for navigation
  static const amber = Color(0xFFFFD166);

  // Backgrounds & text
  static const bg = Color(0xFFE3F2FD); // light blue background
  static const text = Color(0xFF2B2B2B); // charcoal

  // States
  static const success = Color(0xFF4CAF50);
  static const danger = Color(0xFFE63946);

  // Education screen contrast (teal science look)
  static const eduTeal = Color(0xFF004D4D);

  static const cardShadow = Color(0x1A000000); // 10% black for soft shadow
}
